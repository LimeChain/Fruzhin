[settings]
timeout = 35

[relaychain]
chain = "westend-local"
default_command = "zombienet/start-node.sh"
default_args = ["-n westend --node-mode light --sync-mode warp -dbc"]
chain_spec_path = "genesis/westend-raw.json"

[[relaychain.nodes]]
name = "alice"
validator = false
